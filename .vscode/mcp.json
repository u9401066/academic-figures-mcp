{
  "servers": {
    "academic-figures": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${workspaceFolder}",
        "python",
        "-m",
        "src.server"
      ],
      "envFile": "${workspaceFolder}/env"
    }
  }
}
