# Decision Log

| Date | Decision | Rationale |
| ---- | -------- | --------- |
| 2026-04-10 | Adopt a dual-deliverable architecture: a local MCP server for agent orchestration and a VS Code extension for Copilot/human workflows, with a structured style preset registry as a core shared layer. | The current repo already has prompt and standards knowledge, but it lacks a reusable data layer for style conversion and a user-facing shell. Making presets a shared structured layer lets both the MCP server and the extension resolve the same journal/style/domain defaults and keeps style transfer explainable and extensible. |
| 2026-04-10 | VS Code extension 以設定切換 google/openrouter provider，並使用 SecretStorage 注入對應 API key 與 OpenRouter headers 到 MCP 啟動環境。 | 這讓本機工作區不必保存明文金鑰，同時讓 OpenRouter 模型 `google/gemini-3.1-flash-image-preview` 與直接 Google Gemini 模式共用同一套 VSX 啟動流程。 |
| 2026-04-10 | 專案與 VS Code extension 一律採用 Apache License 2.0，並在 repo root 與 extension 子專案各自放置一份 LICENSE 檔。 | root LICENSE 可覆蓋整體專案授權，extension-local LICENSE 可讓 vsce 在子專案目錄內打包時正確辨識授權，避免使用 skip-license 繞過缺少授權檔的問題。 |
| 2026-04-10 | The VS Code extension now stores Ollama endpoint/model settings but does not expose Ollama as a working image provider for the Academic Figures runtime. | The Python MCP server currently supports runtime image generation only through Google Gemini and OpenRouter. Storing Ollama settings in the extension is useful for future expansion and external workflows, but presenting it as an active image provider today would be misleading. |
| 2026-04-10 | Academic Figures MCP should be positioned primarily as a complete agent harness for publication-ready figure generation workflows, not merely as a wrapper around an image-generation API. | The core differentiator is the end-to-end workflow: PMID ingestion, figure-type planning, prompt orchestration, quality evaluation, MCP exposure, and VSX packaging so non-engineers can use it quickly. API connectivity is an implementation detail, not the main product story. |
| 2026-04-10 | Do not expand the local PubMed client into a full academic-search middleware; keep search/discovery as an external or sidecar capability and keep this repo focused on figure workflows. | `pubmed-search-mcp` already covers unified literature search, PICO, citation exploration, full-text, and figure extraction. Rebuilding those capabilities here would blur product boundaries and dilute the figure-generation harness. |
| 2026-04-10 | Treat this repository as greenfield and remove legacy compatibility shims instead of preserving old import paths. | No code in the repo depends on the top-level wrapper modules, and the canonical runtime already lives under `src.presentation` and `src.infrastructure`. Keeping dead compatibility layers would only create drift and confuse future contributors. |
| 2026-04-10 | Treat regenerated *.egg-info metadata as build noise and suppress it with workspace settings instead of repeatedly deleting it. | uv/setuptools workflows can recreate egg-info during normal validation. Hiding *.egg-info from Explorer/search and excluding it from markdownlint removes editor noise without changing packaging behavior. |
| 2026-04-11 | Align workspace `.vscode/mcp.json` with the official VS Code MCP schema by using `type: "stdio"` and `envFile`, while keeping PowerShell only for workspace directory switching. | The current VS Code documentation explicitly supports `envFile` for stdio servers and recommends avoiding hardcoded secrets in `mcp.json`. This keeps the shared config closer to the platform model and avoids custom secret-handling logic in the workspace config itself. |
| 2026-04-11 | Keep [scripts/start_afm_local.ps1](scripts/start_afm_local.ps1) as the manual bootstrap path for local `afm-server` and `afm-run` flows. | The wrapper is still useful outside `mcp.json` because local shells do not automatically load the repo-root `env` file. It provides one repeatable entrypoint for manual testing, smoke runs, and direct figure generation. |
| 2026-04-11 | Public bitmap creation should converge on one generic generation tool, while PMID intake, planning, evaluation, presets, and workflow controls remain harness layers around it. | The product moat is the workflow harness rather than multiple competing render entrypoints. A single generic generation contract can later support academic figures, icons, and other visual assets, while domain-specific tools keep shaping, validating, and sequencing requests upstream. |
| 2026-04-11 | Implement `generate_figure` as a dual-input contract that already accepts generic `planned_payload`, while keeping direct PMID generation only as a backward-compatible bridge. | This lets the repo start using one public renderer immediately without breaking existing CLI, tests, or extension flows. New asset types such as repo icons can now use the same generation tool through a payload-file path. |
| 2026-04-10 | Use a YAML-backed journal registry as the source of truth for figure-house-style constraints and inject them from the MCP layer. | This removes the need for agents to memorize journal rules, keeps prompt modifications deterministic, and allows planners/generators to switch journal standards through a data file instead of hard-coded prompt text. |
| 2026-04-11 | Maintain root ROADMAP.md and CHANGELOG.md as first-class project documents in addition to Memory Bank notes. | Product direction and shipped changes should be visible to contributors and users directly in the repository, not only in internal Memory Bank files. |
| 2026-04-11 | Standardize repo-level MCP startup on a shell-neutral `uv run --project ... python -m ...` pattern and provide a Python local launcher for manual use. | The static workspace config must not depend on PowerShell if the repo is expected to work unchanged on Windows, macOS, and Linux. Keeping the launch path in `uv` and Python preserves one portable startup contract while allowing the PowerShell helper to remain as a Windows convenience wrapper. |
| 2026-04-11 | Keep direct PMID input only as a compatibility bridge and force `generate_figure` itself to render from a planning payload. | Planning and generation should no longer diverge into separate code paths. Keeping one render contract reduces drift, preserves reusable `planned_payload` artifacts, and makes explicit planning the canonical workflow without breaking existing callers immediately. |
| 2026-04-14 | Composite figure assembly must follow the same Presentation -> Application -> Domain interface -> Infrastructure path as the other MCP tools. | The previous presentation-level implementation duplicated orchestration logic and bypassed the DDD boundary. A dedicated CompositeFigureUseCase keeps tool handlers thin, makes the route testable, and aligns composition with the existing DI/governance model. |
| 2026-04-14 | Publish Python releases to PyPI through GitHub Actions OIDC Trusted Publisher, and publish the VS Code extension from the same tag-triggered workflow. | This removes long-lived PyPI API tokens, keeps release execution tied to reviewed Git tags, and gives Python package and VS Code Marketplace artifacts one consistent release contract. |
| 2026-04-14 | Determine output image media type from the actual returned bytes, not from the filename or a hard-coded `.png` assumption. | Gemini/OpenRouter can return JPEG bytes even when the surrounding workflow expects a PNG path. Detecting media type from signatures at the adapter boundary keeps file extensions, edit outputs, smoke tests, and committed assets consistent with the real payload. |
| 2026-04-14 | Keep MetadataFetcher as the narrow application boundary, and make offline/local corpora a separate file-backed infrastructure adapter selected by config. | This delivers the first real pluggable metadata source without coupling the figure runtime to MCP transport, PyPI search libraries, or richer research payloads. It also improves smoke tests and demos by allowing deterministic local paper inputs. |
| 2026-04-14 | AFM requires a patch release after VSX smoke testing because the published extension manifest was missing mcpServerDefinitionProviders, which breaks activate() in the extension host. | Backend smoke passed, but extension-host smoke exposed a real runtime failure in the published VSX package. The manifest fix is minimal, validated locally, and user-facing, so it should ship as a patch release rather than wait for a larger feature batch. |
| 2026-04-14 | AFM should document uvx-based package mode as the primary cross-platform install path, following the Zotero Keeper pattern. | `uvx --from academic-figures-mcp afm-server` is shell-neutral and avoids repo-path, quoting, and platform-specific launcher issues. Local checkout mode remains for development, but package mode is the safest default for macOS/Linux/Windows users. |
| 2026-04-14 | The package smoke workflow must force UTF-8 output for the uvx child process. | Windows GitHub Actions runners default to cp1252 console encoding, and AFM planning output includes Unicode arrows and CJK-safe text guidance. Forcing UTF-8 keeps the cross-platform smoke deterministic without changing user-facing package behavior. |
| 2026-04-14 | VS Code local-source MCP launches must go through src.server, not python -m src.presentation.server. | Launching src.presentation.server with -m makes that file run as __main__, while the tool decorators import src.presentation.server by canonical name. That creates two FastMCP instances: one empty instance that runs, and one populated instance that never serves requests. Using the src.server shim ensures only the canonical src.presentation.server module owns the live FastMCP instance. |
| 2026-04-15 | Copilot research workflow guidance should be enforced through lightweight runtime hooks backed by one shared tool-policy file. | The shell and PowerShell hook scripts cover prompt analysis, pipeline enforcement, result evaluation, and session lifecycle. Keeping the MCP surface and workflow rules in one JSON policy reduces drift between prompts, hooks, and reusable skills. |
| 2026-04-15 | Make plan_figure dual-entry: PMID-backed metadata fetch when pmid is provided, otherwise synthesize planning context from generic source_title/source_kind/source_summary/source_identifier inputs. | AFM generation already supports generic planned_payload jobs, so the cleanest fix is to broaden the planning boundary rather than force preprints, repositories, or briefs through a PubMed-only route. This preserves the existing PMID path while making planning match the real product scope. |
| 2026-04-15 | Expose output_format as a user-facing tool parameter and perform raster conversion inside MCP instead of forcing hosts to post-process provider outputs. | The previous behavior made file type provider-dependent. Adding output_format at the tool boundary keeps the public contract host-friendly while reusing one shared conversion path across generation and editing. SVG remains pass-through only because safe rasterization would require additional rendering infrastructure. |
| 2026-04-15 | Use generate_figure as the default public single-entry drawing surface, with internal plan-first orchestration for PMID and generic source briefs, while retaining plan_figure as an advanced optional tool. | This keeps the host-facing workflow simple and reduces tool-selection burden for agents, without sacrificing explicit planning and inspection capabilities needed by power users or debugging flows. |
| 2026-04-15 | 將 output-format conversion 從 application helper 重構為 domain OutputFormatter 介面，並由 infrastructure PillowOutputFormatter 實作後透過 DI 注入 GenerateFigureUseCase 與 EditFigureUseCase。 | 這能恢復 DDD 分層，避免 application 直接依賴 Pillow 和檔案 I/O；同時讓 output-format normalization、media-type 推導與 raster conversion 集中在單一基礎設施服務，便於未來替換 provider 或擴充格式支援。 |
| 2026-04-15 | 圖片自評採用『MCP 內部自動執行 + structured result 直接回傳 host/Copilot + 保留手動 verify/evaluate 工具』的雙模式 contract。 | 自動 quality gate 讓高階 generate_figure 維持單一入口並減少 host orchestration；把結果直接放進 tool response 可讓 Copilot 立即決策是否要 edit/retry；獨立的 verify_figure/evaluate_figure 則保留人工重跑、審核與除錯能力。 |
| 2026-04-15 | 圖片 review 採雙路徑 harness：provider_vision 由 MCP 內部自動 quality gate 執行並持久化，host_vision 保留給 Copilot/宿主模型外部看圖；整體 acceptance policy 為至少一條自評通過。 | Google/Gemini provider 具備穩定可重播的內部自評能力，適合做預設自動 gate 與 manifest 持久化；Copilot/host 可能同時具備視覺能力，適合補充外部 review 與人工判讀。因此 contract 不應綁死單一路線，而應明確記錄 accepted routes、已執行狀態與最低通過門檻。 |
| 2026-04-15 | 雙路徑 review harness 不只描述 accepted routes，還要提供 host_vision 的回寫入口；因此新增 record_host_review tool，讓 Copilot/宿主模型在看圖後可把 verdict 寫回 manifest。 | MCP 內部無法主動驅動外部 Copilot 視覺模型，但可以穩定提供 manifest-based write-back API。這讓 provider_vision 保持自動化與可重播，同時讓 host_vision 成為真正可追蹤、可持久化的第二條 review 路，而不是只存在文件上的概念。 |
| 2026-04-15 | manifest inspection 不只提供 list summary，而是新增 get_manifest_detail 並將 review_history 視為一級資料結構持久化。 | 雙路徑 review harness 如果只有 current review_summary，host 無法可靠追蹤 provider 與 host 的歷史 verdict，也不利於 lineage-based debug。將 review_history 持久化並提供 load-by-id detail tool，才能讓 host 直接讀到完整 review timeline，而不是自行解析 manifest JSON 或猜測祖先狀態。 |
| 2026-04-15 | 雙路徑 review harness 的 acceptance policy 改為 provider_vision_required_host_optional。 | provider-side vision review 是 MCP 內部可重播、可持久化、可批次執行的穩定基線，因此應作為通關條件；host-side review 依賴外部 Copilot/宿主模型，適合作為第二意見與補充訊號，但不應讓外部判讀覆蓋內部 baseline gate。 |
| 2026-04-15 | 將 repo 體檢優先級定為先修工具檢查已報錯的項目（mypy + failing pytest）再評估進一步優化。 | 先消除真實紅燈可快速恢復穩定基線並避免後續優化建立在不一致狀態上；修復後全量測試通過，表示當前主線可用。 |
| 2026-04-15 | 將 gif 納入 output_format 正式支援格式；目前常見交付格式 contract 為 png、gif、jpg/jpeg、webp 的自動 raster 轉檔，加上 svg 的 pass-through only。 | 使用者明確要求常見格式完整性。既有架構已涵蓋 png/jpeg/webp/svg，缺口只在 gif，因此以最小變更擴充 validation、formatter 與測試即可完成常見 web/image delivery 格式閉環。 |
| 2026-04-15 | 所有本機 CLI / extension local-source 啟動入口都必須先經過 bootstrap，再匯入 presentation 模組。 | safe cwd 修復如果只覆蓋 server 而漏掉 direct-run，extension 指令與 package 指令的行為就會分岔。統一經過 bootstrap 可以確保 invalid cwd 修復、PWD 同步與未來啟動前置檢查只維護一條鏈。 |
| 2026-04-15 | package-mode wheel 必須把 templates 與 journal profile 資產一併封裝，不能依賴 repo-root templates 目錄存在。 | uvx / installed wheel 在 repo 外執行時，PromptEngine 與 JournalRegistry 仍會讀取 templates 路徑。把 templates 納入 wheel data-files 後，package-mode planning 才能真正自洽，不會只在原始碼工作樹內正常。 |
| 2026-04-15 | 公開 GenerationResult 與主要 application response 採雙層 contract：內部用 enum 型別化 status/error_kind，外部保留既有字串 status/error 相容欄位。 | 直接把 public tool response 改成 enum 物件會破壞 JSON/MCP 相容性，但完全只留自由字串又無法提供穩定型別。用 enum 做內部來源、在 response 序列化為 `result_status` / `error_kind` 字串，可同時滿足型別化與向後相容。 |
| 2026-04-15 | review_harness、verify_figure 與 multi_turn_edit 也採相同策略：內部用 typed review enums 統一 route/outcome 狀態，外部保留既有 passed/status/summary 等相容欄位並額外序列化 `review_route`、`review_status`、`route_status`。 | review metadata 原本散落在 quality_gate、review_summary routes、review_history entry 與 verify/multi-turn 回傳 dict 中，各自以自由字串表達。統一為 enum 驅動的序列化欄位後，host/extension 可以穩定消費 review 狀態，同時不破壞既有 MCP JSON 契約。 |
| 2026-04-15 | manifest list/detail 不直接回傳 raw persisted review dict，而是先經 application serializer 正規化後再對外輸出；presentation inventory 同步公開這些 typed 欄位。 | persisted manifests 可能來自舊版本，缺少 `route_status`、`review_status` 等新欄位。若 list/detail 直接透傳，host 端會看到 schema 漂移。把正規化放在 application serializer，可同時兼容舊 manifest 與新 manifest，而不需要改動持久化格式或 extension runtime。 |
| 2026-04-15 | 所有直接回傳 review metadata 的 tool response 都應走同一層 review public serializer；persisted builders 也要補齊 compatibility 欄位，避免 public-normalized payload 與 freshly-saved manifest 漂移。 | 只修 manifest/detail 或單一 tool response 會留下 raw `quality_gate`/`review_summary`/`review_history` 漏洞。將 generate/replay/retarget/record_host_review 全接到同一層 serializer，並讓 build_review_summary/build_*_review_entry 直接產出完整欄位，可同時消除單點手組 dict 與 stored-vs-public schema drift。 |
| 2026-04-15 | 在 extension 成為 manifest/detail payload 的直接 consumer 之前，只在 presentation inventory 公開 TypeScript 映射指引，不預先加入未被使用的 TS runtime types。 | 目前 extension 尚未讀取這些 payload，先新增未使用型別只會增加維護成本。把 typed schema 與 mapping guidance 放進 inventory，可先固定 contract，等 extension 真正消費時再做 1:1 TypeScript 介面映射。 |
| 2026-04-15 | host-facing error payload 也採 dedicated serializer：用 `error_status` / `error_category` 補齊 typed contract，但保留既有 `status` / `error` 與 result-based failure 的 `result_status` / `error_kind` 相容欄位。 | 目前錯誤面分散在 presentation `_error_payload()`、generate 的 early-return failure、以及 result-based failure responses。把它們收斂到同一個 error serializer，可讓 host 與未來 extension 讀取錯誤狀態時有穩定欄位，而不必依賴每個 tool 各自手組的字串。 |
| 2026-04-15 | extension 的 TypeScript 型別映射應與 payload 消費行為同時落地，而不是先建立未使用的 interface。 | 現在 extension 仍以 `Record<string, unknown>` 接 direct-run JSON，尚未直接消費 manifest/detail payload。先把 review/error contract 固定在 inventory 與 MCP response，等 extension 真正讀這些 payload 時再一起建立 TS 介面，可以避免未使用型別漂移。 |
| 2026-04-15 | batch/aggregate response 也採 dedicated aggregate serializer：先固定 `aggregate_kind` / `aggregate_status` / `item_count` 為 core contract，batch_generate 再額外序列化 `total_count` / `success_count` / `failed_count`。 | 目前 host-facing aggregate surface 主要是 batch_generate 與 list_manifests。若繼續各自手組 summary 欄位，host 與未來 extension 仍要依賴零散自由字串。把 aggregate summary 收斂到共享 serializer，可與既有 review/error contract 形成一致的 public schema 層，同時保留 legacy `total` / `success` / `failed` 相容欄位。 |
| 2026-04-23 | 新增 publication image preparation 必須是純 code/Pillow 路徑，並以 `generation_used=false` 明確宣告不使用生成模型。 | 使用者需要把既有圖片處理成學術發表常見 600 DPI 交付檔，而不是重新生成圖片。將此能力放在獨立 PublicationImageProcessor port + Pillow infrastructure，可避免誤觸 Gemini/OpenRouter/Ollama，並讓 host 能從 response 中稽核處理路徑。 |
| 2026-04-23 | 600 DPI 工具必須區分「重採樣到最終印刷尺寸」與「只寫入 DPI metadata」。 | DPI 只有在搭配最終印刷尺寸時才等於可交付像素需求；單純改 metadata 不能增加解析度或科學細節。工具在未提供 `width_mm` / `height_mm` 時保留原像素並回傳 warning，避免對學術發表品質做誤導性承諾。 |
| 2026-04-23 | provider selection 必須 fail closed，未知 `AFM_IMAGE_PROVIDER` 不可靜默 fallback 到 Google。 | 發布 OpenAI provider seam 後，使用者很可能設定 `openai` 或手動輸入 provider 值；若拼錯或未註冊 provider 被當成 Google 會產生誤導性錯誤與錯誤成本。明確 configuration error 比隱性 fallback 安全。 |
| 2026-04-23 | `output_size` 必須作為結構化 provider hint 往下傳，而不只寫在 prompt 裡。 | OpenAI Images API 的 `size` 是正式參數，未來不同 provider 也可能需要 quality/background/format 等結構化選項。先把 `output_size` 接到 ImageGenerator/runtime boundary，可讓 `gpt-image-2` path 不靠 prompt 猜尺寸。 |
| 2026-04-23 | OpenAI image generation 接入以 `gpt-image-2` Images API 為 image model，以 Responses API vision model 做 review。 | 官方模型頁顯示 `gpt-image-2` 支援 image generation/edit endpoints；但 provider-side review 需要 text-capable vision output，因此獨立使用 `OPENAI_VISION_MODEL`，避免把 image-only model 當成評審模型。 |
