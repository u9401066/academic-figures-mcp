# Active Context

## Current Goals

- Verify whether Marketplace and Open VSX eventually reflect the already-successful 0.4.4 publish workflow.
- Preserve the final release state: v0.4.4 tag and publish workflow are complete, but public registry visibility still appears delayed.

## Current Blockers

- No local code blockers. Remaining uncertainty is external: public registry visibility still lags even though the GitHub publish workflow completed successfully.
